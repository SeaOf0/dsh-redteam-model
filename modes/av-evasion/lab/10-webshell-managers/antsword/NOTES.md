# 蚁剑（AntSword）魔改免杀

- 架构面：开源（JS 插件生态）——编码器/解码器全可自定义，**流量层魔改成本最低**。
- 魔改三路：①自定义编码器（本 demo：默认 base64/chr 特征弃用→AES+业务参数伪装；
  进阶=流量转换跳过密钥交互）②马侧一句话型配合（连接参数名/函数面改造）③RSA 模式
  （自解码器路线）。
- 检测侧配对：默认编码器特征串（弃用即失效）；异常参数结构（业务名+高熵值）；
  回显行为模型；马侧一句话静态查杀（D 盾语料面）。
- 判定表：| WAF/D 盾 | 结果 | 原文行 |
